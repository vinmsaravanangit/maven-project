pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 mvn.exe 'mvn clean package'
            }
            post{
                success{
                    echo 'Now archiving...'
                    archiveartifacts artifacts: '**/target/*.war'
                }
            }

        }

        }
}